# ALFAZAA / الفزعة

A polished Flutter UI prototype for ALFAZAA, built with mock data only.

## Run

```bash
flutter pub get
flutter run -d chrome
```

This prototype intentionally does not use Firebase, backend services, or real map APIs.
